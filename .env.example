# Это пример файла с переменными окружения.
# Скопируйте его в .env и заполните своими данными.
# Файл .env НЕ должен попадать в систему контроля версий.

# Настройки Django
SECRET_KEY=your-super-secret-key-goes-here
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Настройки базы данных
# Для SQLite: DATABASE_URL=sqlite:///db.sqlite3
# Для PostgreSQL: DATABASE_URL=psql://user:password@host:port/dbname
DATABASE_URL=sqlite:///db.sqlite3

# Redis (для Celery)
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Email (для отправки уведомлений)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# Для реальной отправки используйте, например:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=user@example.com
# EMAIL_HOST_PASSWORD=password

# Рекомендации:
# - Обновлять список переменных по мере развития проекта
